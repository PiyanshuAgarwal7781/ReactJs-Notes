//General:
// . The page should not reload when the link in header are clicked so we use impoer {link} from 'react-router-dom'
// . <a href=""></a> is replaced by <Link to="Path you want to give"></Link>

// API STORING
// . json-server is a free api used to frontend 
//  json-server --watch db.json-> to start json server 
//  "user":[] array of object is created 

// now in reat file copy the url "http://localhost:3000/user"
// post request method: This is used to store the data in api 
// fetch(url,
// {method:"POST",
// headers:{"Content-Type":'application/json'},
// body:JSON.stringify(userobj),}
// )

// every time the information is stored in from of object, and when we send get request it gives in the form of array
