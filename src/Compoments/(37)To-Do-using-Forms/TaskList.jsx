function TaskList(props){
    return(
        <div>
            <h2> Task List  </h2>
            {/* {props.task.map((i, index) => (
  <p key={index}>{i}</p>
))} */}

        </div>
    )
}

export default TaskList;